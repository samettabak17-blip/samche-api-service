name: Staging Assistant Generation Read-Only Audit

on:
  workflow_dispatch:
    inputs:
      tenant_id:
        description: Exact non-secret staging tenant UUID (optional when name/prefix are supplied)
        required: false
        type: string
      profile_version_id:
        description: Exact non-secret ACTIVE Business Profile version UUID (optional when name/prefix are supplied)
        required: false
        type: string
      tenant_name:
        description: Exact staging tenant name, used only to resolve one read-only audit scope
        required: false
        type: string
      profile_version_prefix:
        description: Non-secret ACTIVE Business Profile version ID prefix
        required: false
        type: string

permissions:
  contents: read

jobs:
  audit:
    if: github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm install --ignore-scripts
      - name: Run strict-TLS read-only Assistant generation audit
        env:
          STAGING_DATABASE_URL: ${{ secrets.STAGING_DATABASE_URL }}
          TASK6_AUDIT_TENANT_ID: ${{ inputs.tenant_id }}
          TASK6_AUDIT_PROFILE_VERSION_ID: ${{ inputs.profile_version_id }}
          TASK6_AUDIT_TENANT_NAME: ${{ inputs.tenant_name }}
          TASK6_AUDIT_PROFILE_VERSION_PREFIX: ${{ inputs.profile_version_prefix }}
        run: node scripts/staging-assistant-generation-audit.js
